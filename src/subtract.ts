/**
 * Subtract two numbers and return the answer
 * 
 * @param a - first number
 * @param b - second number
 * @returns - returns the answer from the subtracted numbers
 * 
 * @public
 */
export function subtract(a:number, b: number): number {
  return a - b;
}